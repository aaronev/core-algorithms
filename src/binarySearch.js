export default function binarySearch (numbers, num){
  for (let i = 0; i < numbers.length; i++) {
    if (num === numbers[i]) {
    }
  }
  for (let i = 0; i < numbers.length; i++) {
      if (num !== numbers[i]) {
    }
  }
}
